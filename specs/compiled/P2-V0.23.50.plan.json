{
  "architecture_decisions": [
    "AC-01"
  ],
  "work_items": [
    "AC-01"
  ],
  "validations": [
    "AC-02",
    "AC-04: validate protected requirement \u2014 Retry must reuse existing VerifiedLineageDelivery checks proving accepted IMPLEMENT and passed VERIFY refer to the same exact current lineage.",
    "AC-05: validate protected requirement \u2014 Retry reuses the existing canonical publication branch, exact lineage commit, matching open PR and exact Preview when already present. It may create only missing bounded publication artifacts.",
    "AC-06: validate protected requirement \u2014 Success or failure leaves Engineering Run state REVIEW, run revision unchanged, protected lifecycle attempt identities unchanged, and `last_failure_code` unchanged. A successful publication may add/replay only the existing durable `SOURCE_DELIVERY` record used by the accepted delivery contract.",
    "AC-07: validate protected requirement \u2014 Successful retry records/replays one SOURCE_DELIVERY success fact for the exact lineage. Failed retry may record bounded delivery failure evidence but cannot be presented as a run-stage failure.",
    "AC-08: validate protected requirement \u2014 At REVIEW with `vercel-preview`, the delivery panel shows current delivery status. When not published it exposes `Retry Vercel Preview`; on success it exposes safe `Open Vercel Preview` and `Open GitHub PR` links/actions without saying merged or deployed.",
    "AC-09: validate protected requirement \u2014 The delivery action calls only the delivery retry API. It does not call resume/autonomous continuation.",
    "AC-10: validate protected requirement \u2014 At desktop widths, the Live Build workspace root is vertically scrollable so header, pipeline, tabs, primary content and contextual content are reachable without browser zoom or layout clipping.",
    "AC-11: validate protected requirement \u2014 Event stream, code, diff, evidence and horizontal technical panes preserve their existing bounded inner scrolling and Follow Live behavior.",
    "AC-12: validate protected requirement \u2014 The existing compact/mobile root scroll and mobile Build/Review flows remain unchanged."
  ],
  "risks": [
    "AC-03"
  ],
  "spec_id": "P2-V0.23.50",
  "protected_acceptance_map": [
    {
      "id": "AC-01",
      "title": "REVIEW delivery status is read-only and exact",
      "protected_requirement": "A read endpoint returns `NOT_PUBLISHED` or `PUBLISHED` for the exact current REVIEW run and canonical Project delivery mode. Published status may expose only bounded safe PR/Preview URLs and provider identifiers from a durable verified delivery record."
    },
    {
      "id": "AC-02",
      "title": "Retry is REVIEW-only",
      "protected_requirement": "Delivery retry rejects non-REVIEW runs and historical/unbound runs before any provider mutation."
    },
    {
      "id": "AC-03",
      "title": "Retry never reruns protected lifecycle stages",
      "protected_requirement": "A delivery retry must not invoke AutonomyCoordinator and must not create PLAN/IMPLEMENT/BUILD/TEST/VERIFY attempts."
    },
    {
      "id": "AC-04",
      "title": "Exact verified lineage remains authoritative",
      "protected_requirement": "Retry must reuse existing VerifiedLineageDelivery checks proving accepted IMPLEMENT and passed VERIFY refer to the same exact current lineage."
    },
    {
      "id": "AC-05",
      "title": "Provider publication is replay-safe",
      "protected_requirement": "Retry reuses the existing canonical publication branch, exact lineage commit, matching open PR and exact Preview when already present. It may create only missing bounded publication artifacts."
    },
    {
      "id": "AC-06",
      "title": "Run authority remains unchanged",
      "protected_requirement": "Success or failure leaves Engineering Run state REVIEW, run revision unchanged, protected lifecycle attempt identities unchanged, and `last_failure_code` unchanged. A successful publication may add/replay only the existing durable `SOURCE_DELIVERY` record used by the accepted delivery contract."
    },
    {
      "id": "AC-07",
      "title": "Delivery events remain truthful",
      "protected_requirement": "Successful retry records/replays one SOURCE_DELIVERY success fact for the exact lineage. Failed retry may record bounded delivery failure evidence but cannot be presented as a run-stage failure."
    },
    {
      "id": "AC-08",
      "title": "Client exposes explicit delivery recovery",
      "protected_requirement": "At REVIEW with `vercel-preview`, the delivery panel shows current delivery status. When not published it exposes `Retry Vercel Preview`; on success it exposes safe `Open Vercel Preview` and `Open GitHub PR` links/actions without saying merged or deployed."
    },
    {
      "id": "AC-09",
      "title": "Retry UI does not reuse generic build continuation",
      "protected_requirement": "The delivery action calls only the delivery retry API. It does not call resume/autonomous continuation."
    },
    {
      "id": "AC-10",
      "title": "Desktop observability vertically scrolls",
      "protected_requirement": "At desktop widths, the Live Build workspace root is vertically scrollable so header, pipeline, tabs, primary content and contextual content are reachable without browser zoom or layout clipping."
    },
    {
      "id": "AC-11",
      "title": "Nested observability panes remain usable",
      "protected_requirement": "Event stream, code, diff, evidence and horizontal technical panes preserve their existing bounded inner scrolling and Follow Live behavior."
    },
    {
      "id": "AC-12",
      "title": "Mobile behavior does not regress",
      "protected_requirement": "The existing compact/mobile root scroll and mobile Build/Review flows remain unchanged."
    }
  ],
  "critique": [
    ":\n- IMPLEMENT, BUILD, TEST and VERIFY succeeded on one immutable accepted lineage.\n- The run reached REVIEW revision 6 with `last_failure_code=null`.\n- Vercel Preview delivery failed and emitted `SOURCE_DELIVERY_FAILED`.\n- No delivery-only retry action exists.\n- Desktop observability uses a fixed-height root with only nested-pane scrolling, so lower content can be inaccessible.\n- P2-V0.23.50 adds an explicit REVIEW-only delivery recovery surface and correct desktop scroll containment."
  ],
  "dspy_run": {
    "executed": true,
    "model": "ollama_chat/qwen2.5:0.5b",
    "api_base": "http://127.0.0.1:11434",
    "local_development_view": true
  }
}
