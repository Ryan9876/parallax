{
  "architecture_decisions": [
    "A valid GitHub Actions OIDC token whose claims identify repository `Ryan9876/parallax-qa`, ref `refs/heads/main`, workflow `production-replay.yml@refs/heads/main`."
  ],
  "work_items": [
    "The new standing workflow is exactly `Ryan9876/parallax-qa/.github/workflows/production-replay.yml@refs/heads/main`."
  ],
  "validations": [
    "No validation is required for this architecture decision.",
    "AC-01: validate protected requirement \u2014 A valid GitHub Actions OIDC token whose claims identify repository `Ryan9876/parallax-qa`, ref `refs/heads/main`, workflow `Ryan9876/parallax-qa/.github/workflows/production-replay.yml@refs/heads/main`, GitHub-hosted runner, admitted event, and correct audience is accepted.",
    "AC-02: validate protected requirement \u2014 A token using the `parallax-qa` repository with an old `Ryan9876/parallax` workflow reference, or the application repository with the new `parallax-qa` workflow reference, is rejected.",
    "AC-03: validate protected requirement \u2014 During this admission release only, the two existing exact main-branch application-repository QA workflow identities continue to authenticate so rollback and migration validation remain available.",
    "AC-04: validate protected requirement \u2014 Wrong ref, self-hosted runner, unadmitted event name, wrong audience, invalid signature/standard claims, missing run id, untrusted repository, or untrusted workflow pair is rejected.",
    "AC-05: validate protected requirement \u2014 Successful new-repository OIDC authentication maps to the existing bounded QA session/principal and does not create new Project, Engineering Run, source-lineage, model-routing, Git/deployment, or REVIEW authority.",
    "AC-06: validate protected requirement \u2014 The standing `parallax-qa` workflow contains routine Python and OT Time source-only acceptance only; it does not include standing W9-S1 greenfield initialization authority.",
    "AC-07: validate protected requirement \u2014 After deployment, the exact `parallax-qa` workflow authenticates against production and both routine jobs reach `REVIEW` with no failure and validate authenticated source-only ZIP handoff before old workflow trust is removed.",
    "AC-08: validate protected requirement \u2014 The verifier uses no repository prefix, workflow prefix, glob, substring, actor-only, branch-only, or event-only acceptance rule.",
    "AC-09: validate protected requirement \u2014 `ARCHITECTURE.md` records exact repository/workflow-pair authorization and the dedicated QA repository boundary as v3.29.",
    "AC-10: validate protected requirement \u2014 Exact-head API/contracts, client/browser/Skia, Bounded Autonomy, protected promotion, Workstream Spec Validation, and DSPy release compilation pass before merge."
  ],
  "risks": [
    "No material risk and mitigation is necessary for this architecture decision."
  ],
  "spec_id": "P2-V0.23.17",
  "protected_acceptance_map": [
    {
      "id": "AC-01",
      "title": "Exact dedicated QA pair is admitted",
      "protected_requirement": "A valid GitHub Actions OIDC token whose claims identify repository `Ryan9876/parallax-qa`, ref `refs/heads/main`, workflow `Ryan9876/parallax-qa/.github/workflows/production-replay.yml@refs/heads/main`, GitHub-hosted runner, admitted event, and correct audience is accepted."
    },
    {
      "id": "AC-02",
      "title": "Repository and workflow are bound as one pair",
      "protected_requirement": "A token using the `parallax-qa` repository with an old `Ryan9876/parallax` workflow reference, or the application repository with the new `parallax-qa` workflow reference, is rejected."
    },
    {
      "id": "AC-03",
      "title": "Existing migration identities remain temporarily valid",
      "protected_requirement": "During this admission release only, the two existing exact main-branch application-repository QA workflow identities continue to authenticate so rollback and migration validation remain available."
    },
    {
      "id": "AC-04",
      "title": "Existing claim guards remain fail-closed",
      "protected_requirement": "Wrong ref, self-hosted runner, unadmitted event name, wrong audience, invalid signature/standard claims, missing run id, untrusted repository, or untrusted workflow pair is rejected."
    },
    {
      "id": "AC-05",
      "title": "QA principal authority is unchanged",
      "protected_requirement": "Successful new-repository OIDC authentication maps to the existing bounded QA session/principal and does not create new Project, Engineering Run, source-lineage, model-routing, Git/deployment, or REVIEW authority."
    },
    {
      "id": "AC-06",
      "title": "Dedicated QA repository routine replay is bounded",
      "protected_requirement": "The standing `parallax-qa` workflow contains routine Python and OT Time source-only acceptance only; it does not include standing W9-S1 greenfield initialization authority."
    },
    {
      "id": "AC-07",
      "title": "Production cutover is behaviorally proven before contraction",
      "protected_requirement": "After deployment, the exact `parallax-qa` workflow authenticates against production and both routine jobs reach `REVIEW` with no failure and validate authenticated source-only ZIP handoff before old workflow trust is removed."
    },
    {
      "id": "AC-08",
      "title": "No wildcard trust",
      "protected_requirement": "The verifier uses no repository prefix, workflow prefix, glob, substring, actor-only, branch-only, or event-only acceptance rule."
    },
    {
      "id": "AC-09",
      "title": "Architecture records the durable trust topology",
      "protected_requirement": "`ARCHITECTURE.md` records exact repository/workflow-pair authorization and the dedicated QA repository boundary as v3.29."
    },
    {
      "id": "AC-10",
      "title": "Release gates remain green",
      "protected_requirement": "Exact-head API/contracts, client/browser/Skia, Bounded Autonomy, protected promotion, Workstream Spec Validation, and DSPy release compilation pass before merge."
    }
  ],
  "critique": [
    "The new standing workflow is exactly `Ryan9876/parallax-qa/.github/workflows/production-replay.yml@refs/heads/main`."
  ],
  "dspy_run": {
    "executed": true,
    "model": "ollama_chat/qwen2.5:0.5b",
    "api_base": "http://127.0.0.1:11434",
    "local_development_view": true
  }
}
