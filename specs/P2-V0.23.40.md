# Parallax 2.0 — P2-V0.23.40 Honest Static-Web Acceptance Evidence

Status: APPROVED FOR IMPLEMENTATION — SPEC-FIRST
Spec ID: P2-V0.23.40
Date: 2026-09-02
Workstream: #565
Parent: #391 / W9-S1
Architecture target: v3.49
Starting baseline: `d25cb8146c9b993e50aeb18eb0266cee0d875d2f`
Worker branch: `ws/565-static-web-acceptance-evidence`
PR target: `main`

## 1. Objective

Correct the protected evidence semantics exposed by the first real W9-S1 Decision Ledger application. `static-web-v1` BUILD/TEST/VERIFY currently proves only bounded structural properties, but the autonomy coordinator unconditionally labels every protected acceptance criterion as verified after successful TEST and VERIFY. P2-V0.23.40 must make that evidence truthful without weakening structural validation, changing the immutable execution contract, inventing benchmark-specific checks, or bypassing human REVIEW.

For PLAN-bound `static-web-v1` runs, successful structural TEST/VERIFY must explicitly record that all protected acceptance criteria remain unverified by that structural profile. The run may still progress to the existing REVIEW boundary because REVIEW is already the human completion authority, but protected execution must not claim behavioral proof it did not produce.

The persisted runtime-evidence adapter must preserve the same distinction downstream: it may authenticate that static-web TEST/VERIFY commands succeeded against the exact accepted lineage and incorporate the structural-only scope into evidence digests, but it must never reinterpret `acceptance_ids_unverified` as verified behavioral acceptance.

Python and .NET execution evidence retains the existing exact-acceptance verification contract in this slice.

## 2. Verified finding and fixed decisions

- P2-V0.23.39 is merged and production-accepted; frozen W9-S1 source delivery succeeded through exact branch, accepted-lineage commit, GitHub PR and READY Vercel Preview while the Engineering Run remained REVIEW revision 6.
- The delivered Decision Ledger candidate contains only `index.html`, `styles.css` and `app.js`.
- `static_web_validator.py` checks root/file safety, `index.html`, JavaScript syntax and local references. It does not execute CRUD, persistence, import/export, responsive, accessibility or other behavioral acceptance tests.
- `AutonomyCoordinator.run()` currently assigns the full run acceptance map to `acceptance_ids_verified` for TEST/VERIFY whenever execution returns `protected_success=true`, regardless of the selected execution contract's actual verification capability.
- `EngineeringRunService.complete_stage()` then requires that full list and records the stage as passed, converting structural success into protected universal acceptance proof.
- `RuntimeAppBuilderEvidenceAdapter._execution_attempt()` independently assumes TEST/VERIFY acceptance coverage must be present in `acceptance_ids_verified`. Its resulting stage digest is used to authenticate protected command execution and exact-lineage evidence, not to score individual application requirements. The adapter must therefore understand structural-only execution as a distinct authenticated evidence mode without upgrading it to full acceptance verification.
- Source inspection proves the false-positive is material, not semantic nitpicking: the Decision Ledger import validator accepts records with only string `id`, string `title` and a supported `status`; malformed values such as non-array `tags` can therefore be persisted and later break rendering even though the frozen import requirement says invalid import must fail safely without corrupting existing valid data.
- The delivered candidate also contains no automated test files while the frozen objective explicitly requires automated coverage of core CRUD, persistence, search/filter and import behavior.
- The fix must be generic to the closed server-owned execution contract. No benchmark/template ID, acceptance keyword, fixture digest, repository name or candidate content may create the evidence mode.
- `static-web-v1` itself remains unchanged in this slice. Browser/behavioral verification is a later capability unless separately governed.

## 3. Scope

Owned semantic scope:

- `services/api/parallax_api/code/autonomy.py`
  - preserve BUILD `acceptance_ids_targeted` behavior;
  - for TEST/VERIFY under the exact server-resolved `static-web-v1` execution contract, emit an explicit structural-only acceptance envelope rather than all acceptance IDs as verified;
  - derive the writer-side envelope from the execution contract that the coordinator already independently resolved for exact-lineage execution, rather than requiring a new service-facing protocol method;
  - keep Python/.NET TEST/VERIFY evidence unchanged;
  - do not derive the mode from caller/model/candidate evidence.
- `services/api/parallax_api/code/protected.py`
  - add bounded validation for the structural-only acceptance envelope;
  - require exact partition semantics: verified list is empty; unverified and targeted lists exactly equal the protected acceptance map; scope marker is the fixed server value `STRUCTURAL_ONLY`;
  - retain existing exact full-verification validation for non-structural execution.
- `services/api/parallax_api/code/service.py`
  - determine whether partial structural evidence is admissible from the durable passed PLAN execution-contract identity, resolving and verifying that identity through the existing closed catalog before accepting TEST/VERIFY evidence;
  - never trust a stage payload merely because it says `static-web-v1`;
  - preserve existing run-event metadata contracts unless a new field is demonstrably required for acceptance.
- `services/api/parallax_api/evaluation/runtime_evidence.py`
  - authenticate BUILD as the existing exact targeted coverage;
  - authenticate TEST/VERIFY in one of two closed modes: exact full verification, or exact structural-only targeted/unverified coverage with an empty verified list;
  - bind the chosen verification scope and exact acceptance partition into the canonical stage digest so structural-only evidence cannot collide with full-verification evidence;
  - retain observations as statements about protected command result and exact-lineage execution only; do not emit a behavioral-acceptance claim from structural-only evidence.
- `services/api/parallax_api/code/live_observability.py`
  - permit safe projection of the new non-secret structural-only evidence fields.
- focused tests under `services/api/tests/` proving the protected evidence partition, durable PLAN identity requirement, static-web REVIEW progression, forged/static-web evidence rejection on non-static contracts, exact Python/.NET compatibility, runtime-evidence mode separation, safe observability and no authority expansion.
- `ARCHITECTURE.md` v3.49 after focused behavior validation.
- `CURRENT-STATE.md` only after validated release/deployment evidence exists.

## 4. Non-goals

- no browser automation or visual evaluator implementation;
- no claim that structural validation satisfies behavioral acceptance;
- no candidate-authored test trust expansion;
- no keyword or model classification of which acceptance criteria are behaviorally verified;
- no benchmark/template/repository special casing;
- no execution-contract ID, digest, profile digest, command or validation-toolchain change;
- no provider/model/credential/network/package-domain/retry/timeout/candidate-count change;
- no source-lineage, Git, Preview, merge, lifecycle, REVIEW-completion or production-deployment authority expansion;
- no manual patch to the Decision Ledger candidate as a substitute for fixing Parallax.

## 5. Architecture requirements

### 5.1 Structural success and acceptance verification are separate facts

A successful static-web structural TEST/VERIFY proves only that the immutable server-owned static-web command completed successfully against the exact accepted source lineage. It must not imply that the Work Specification's behavioral acceptance criteria were proven.

The structural-only evidence envelope is exactly:

- `acceptance_verification_scope = "STRUCTURAL_ONLY"`;
- `acceptance_ids_targeted` exactly equals the protected acceptance map;
- `acceptance_ids_verified = []`;
- `acceptance_ids_unverified` exactly equals the protected acceptance map.

No duplicates, omissions, additions or alternate scope strings are valid.

### 5.2 The admissible evidence mode is derived from durable PLAN authority

`EngineeringRunService` must independently inspect the latest passed PLAN attempt for the run, parse `ExecutionContractIdentity`, resolve it through the existing closed server catalog, and require exact identity equality before admitting structural-only TEST/VERIFY evidence.

Only resolved `ExecutionContractCode.STATIC_WEB` / `GREENFIELD_STATIC_WEB` may use the structural-only envelope. Missing, malformed, partial, drifted or forged PLAN execution-contract identity cannot authorize structural-only validation and therefore remains subject to the existing full-verification contract or other existing fail-closed execution-contract behavior.

Current-stage evidence cannot self-declare a structural-only privilege.

### 5.3 Writer and validator remain independently authoritative

The autonomy coordinator may use the exact `ExecutionContract` it already resolved for same-lineage execution to select which acceptance envelope it writes. The service must independently re-resolve the durable PLAN identity before it accepts that envelope. This avoids introducing a new coordinator-to-service protocol requirement while preserving defense in depth.

Legacy/pre-lineage executor behavior remains unchanged and cannot opt itself into structural-only evidence.

### 5.4 Full verification semantics remain exact for other contracts

Existing Parallax Python and bounded .NET TEST/VERIFY continue to require `acceptance_ids_verified` to exactly equal the protected acceptance map. P2-V0.23.40 does not broaden partial verification to arbitrary validators or caller-selected tools.

### 5.5 Runtime evaluation preserves, rather than erases, verification scope

The persisted runtime-evidence adapter may accept a successful TEST/VERIFY stage only when its acceptance envelope is exact for one closed mode:

1. **Full verification** — `acceptance_ids_verified` exactly equals the protected acceptance map and no structural-only scope is asserted; or
2. **Structural only** — the fixed `STRUCTURAL_ONLY` scope is present, targeted and unverified IDs both exactly equal the protected acceptance map, and verified IDs are explicitly empty.

For either mode, exact Project/run/lineage/content/sandbox execution invariants remain mandatory. The stage digest must include the mode and exact verified/unverified/targeted partition. This permits evaluation of the existing protected-command/lineage facts without converting structural coverage into behavioral acceptance proof.

### 5.6 REVIEW remains the completion authority

A static-web run with structural-only TEST/VERIFY may still transition through VERIFY to REVIEW when the structural commands pass. REVIEW completion remains governed by the existing `validate_review` requirement: an operator review recommending PASS must verify the exact protected acceptance map against the current implementation workspace digest.

Therefore P2-V0.23.40 reduces false protected claims without adding a new autonomous completion path.

### 5.7 Observability must expose the distinction safely

Durable execution attempt evidence and safe observability may expose the fixed scope plus acceptance targeted/verified/unverified IDs. Existing run-event metadata schemas need not expand merely to duplicate those facts. Hidden reasoning, secrets, provider credentials and unrestricted candidate content remain prohibited.

Existing event identity, lineage, operation, stage outcome and source evidence remain unchanged.

## 6. Security and authority

The structural-only mode is a contraction of claimed authority, not an expansion. It is available only when durable PLAN evidence independently resolves to the exact existing static-web contract. Candidate code, user text, model output at TEST/VERIFY, browser state and request payloads cannot manufacture it.

No new command or external system is introduced. Structural BUILD/TEST/VERIFY still runs under the same sandbox, network policy, source-lineage reconstruction and immutable execution contract. Human REVIEW remains required before completion.

The runtime-evidence adapter remains read-only. Understanding the structural-only envelope gives it no execution, source mutation, provider, review or lifecycle authority and does not allow an unverified acceptance ID to be promoted to verified evidence.

## 7. Reliability, compatibility and rollback

No database migration or execution-contract digest change is allowed. Existing durable PLAN identities therefore remain byte/identity compatible. Runs already at REVIEW remain untouched; new or resumed static-web TEST/VERIFY attempts after the release use truthful structural-only evidence.

Existing evaluation/reference harnesses that prove protected command execution and exact-lineage behavior remain semantically valid when their stage digests preserve the new verification scope. They must not be made green by relabeling structural-only IDs as verified.

Rollback is operationally safe but reintroduces false universal acceptance claims for static-web TEST/VERIFY. Python/.NET behavior must be regression-identical before and after the release.

## 8. Acceptance criteria

### AC-01 W9-S1 false-positive evidence is captured as the root cause

Tests and authoritative records identify the exact existing behavior: the static-web validator is structural, while autonomy currently stamps the complete acceptance map into TEST/VERIFY `acceptance_ids_verified` and the runtime-evidence adapter assumes that field represents all TEST/VERIFY acceptance coverage.

### AC-02 Static-web TEST emits structural-only acceptance evidence

A production-shaped PLAN-bound static-web run with successful TEST records scope `STRUCTURAL_ONLY`, all protected IDs as targeted/unverified, and an empty verified list.

### AC-03 Static-web VERIFY emits structural-only acceptance evidence

The same exact evidence partition is required for successful VERIFY.

### AC-04 Structural-only evidence requires exact durable PLAN identity

Tests prove missing, malformed, partial, drifted or non-static PLAN execution-contract identity cannot authorize the structural-only envelope.

### AC-05 Caller/stage evidence cannot self-authorize partial verification

Tests prove a non-static run or direct protected-stage call that supplies `STRUCTURAL_ONLY` / `static-web-v1` fields without an exact resolved static-web PLAN identity fails closed.

### AC-06 Acceptance partition is exact

Tests reject duplicate, omitted, added or overlapping verified/unverified/targeted IDs and reject any scope value other than the fixed server value.

### AC-07 Python and .NET semantics are unchanged

Existing full-verification paths continue to require `acceptance_ids_verified` to exactly match the protected acceptance map and existing regressions remain green.

### AC-08 Static-web runs still reach human REVIEW

A fully successful structural static-web BUILD/TEST/VERIFY sequence reaches REVIEW, not FAILED or COMPLETE, with explicit unverified acceptance evidence and the existing REVIEW-required event/stop semantics.

### AC-09 REVIEW completion remains exact

Existing review validation still requires recommendation PASS, exact all acceptance IDs verified and the current implementation workspace digest. Structural TEST/VERIFY evidence alone cannot complete REVIEW.

### AC-10 Runtime evaluation preserves verification scope

The runtime-evidence adapter accepts exact full-verification and exact structural-only TEST/VERIFY envelopes as distinct modes, rejects malformed/mixed envelopes, binds the mode and exact acceptance partition into stage digests, and does not emit a behavioral-acceptance claim from structural-only evidence.

### AC-11 Safe observability exposes verification scope without secrets

Protected attempt projection safely exposes structural scope and targeted/verified/unverified IDs while existing secret/private-reasoning/provider-payload guards remain intact. Run-event metadata need not duplicate those facts.

### AC-12 No execution or lifecycle authority expansion

Diff review proves no command, validation profile, execution-contract digest, provider/model/credential/network, source-lineage, Git/Preview, merge, retry, lifecycle, REVIEW-completion or production authority increase.

### AC-13 Protected release gates pass

Spec validation, authentic DSPy SpecCritic/SpecCompiler, strict DSPy plan validation, focused tests, full API regression, Bounded Autonomy, Workstream Spec Validation and P2 CI pass on exact reviewed heads.

### AC-14 Production verification preserves existing runtime health

Exact merged source reaches production READY; normal production preflights pass; canonical `/health` and `/ready` return HTTP 200; exact-deployment runtime scans are clean.

### AC-15 Production-shaped static-web acceptance no longer overclaims

After deployment, a safe new static-web run or bounded production-shaped replay proves successful structural TEST/VERIFY no longer records every acceptance criterion as verified, downstream evidence preserves the structural-only scope, and the run still stops at human REVIEW.

## 9. Release gate

Before substantive implementation:

- `python scripts/validate_spec.py --spec-only specs/P2-V0.23.40.md` passes;
- authentic DSPy SpecCritic and SpecCompiler execute for this exact specification;
- `specs/compiled/P2-V0.23.40.plan.json` is committed and strict `python scripts/validate_spec.py --require-dspy specs/P2-V0.23.40.md` passes.

Before merge:

- focused protected-evidence/static-web/autonomy/service/runtime-evidence/observability tests pass;
- full applicable API and protected release gates pass;
- architecture is reconciled only after focused behavior validation;
- `CURRENT-STATE.md` records only validated, not predicted, progression;
- temporary workflows/harnesses are removed.

After merge:

- post-merge P2 CI and applicable spec validation pass;
- exact production deployment reaches READY and normal release verification passes;
- production-shaped static-web evidence proves the overclaim is removed end-to-end through downstream evidence projection;
- `CURRENT-STATE.md` is finalized from actual evidence.

## 10. Risks

The primary risk is replacing false full verification with a generic partial-verification escape hatch. The mitigation is exact durable PLAN resolution: only the existing closed-catalog static-web contract can admit `STRUCTURAL_ONLY`, and its partition is fixed server-side.

A second risk is breaking old execution-contract identities by adding verification capability to their digest. The mitigation is explicit: no execution-contract/profile digest change is part of this release.

A third risk is fixing persistence while leaving evaluation to silently upgrade structural coverage back into behavioral proof. The mitigation is explicit dual-mode runtime-evidence validation plus digest separation; structural-only evidence remains structural-only downstream.

A fourth risk is reducing product quality by treating truthful partial evidence as sufficient. It is not sufficient for completion: the run still stops at REVIEW and the remaining criteria are explicit. A later governed browser/behavioral verifier may reduce the unverified set, but P2-V0.23.40 does not manufacture that proof.

## 11. Authoritative-record implications

`ARCHITECTURE.md` advances to v3.49 because protected execution and downstream runtime-evidence semantics now distinguish static structural validation from acceptance verification using durable PLAN authority.

`CURRENT-STATE.md` changes only after focused implementation validation and again after actual release/production evidence as needed.

`DESIGN-SYSTEM.md` and `PROJECT-CONSTITUTION.md` do not change.
