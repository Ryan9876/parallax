# Parallax 2.0 — Repository-Aware Protected Validation Toolchains

Status: APPROVED FOR IMPLEMENTATION — SPEC-FIRST; SEMANTIC CODE BLOCKED UNTIL AUTHENTIC DSPy GATE PASSES
Spec ID: P2-V0.23.5
Date: 2026-08-29
Workstream: #421
Control Tower: #391
PR target: `main`

## 1. Objective

Generalize protected candidate and accepted-lineage validation so Parallax can validate supported Python, JavaScript/Node, and .NET applications using deterministic server-owned command profiles selected from bounded repository evidence instead of the current Parallax-repository-specific Python command registry.

Authenticated production replay of the public `github:Ryan9876/ot-time` source-only Project proved P2-V0.23.4 removes the Vercel repository-authorization prerequisite: PLAN passed without `REPOSITORY_AUTHORIZATION_REQUIRED`. The same run then failed before source mutation at IMPLEMENT candidate validation because the protected TEST invocation is hard-coded to Parallax's own pytest paths. The failure is `AUTONOMOUS_IMPLEMENT_FAILED`, `ImplementationContractError`, candidate TEST exit code 4, with `mutation_applied=false`.

This slice fixes that generalization defect without allowing repository manifests, model output, benchmark identity, or user text to become executable command authority.

## 2. Fixed decisions

- Repository compatibility evidence and executable command authority remain separate layers.
- Existing Wave 5 repository intelligence is the deterministic source of ecosystem/root evidence and is extended narrowly to recognize .NET solution/project markers.
- Protected executable commands remain fixed in server-owned released code.
- Initial validation profiles are `python`, `node`, and `dotnet`.
- Profile selection is deterministic from the exact materialized source tree and must fail closed when the repository is unsupported or materially ambiguous.
- Candidate validation and accepted-lineage BUILD/TEST/VERIFY use the same selected profile identity and registered commands.
- Repository-defined package scripts, README instructions, project targets, MSBuild custom commands, shell text, model suggestions, or benchmark identity cannot directly supply executable command strings or arguments.
- BUILD/TEST/VERIFY validation remains offline/deny-all, receives no application environment, cannot install packages from the network, and cannot accept or publish source lineage.
- A profile may define a separate bounded dependency-preparation command when clean source cannot be validated offline. PREPARE is not a lifecycle stage and grants no stage-completion, lineage-acceptance, Git, provider, deployment, or approval authority.
- Dependency preparation runs only in the same disposable source-bound sandbox that will perform validation, with no application environment or credentials and with a server-owned exact outbound package-domain allowlist. The network policy must transition to and be verified as deny-all before BUILD/TEST/VERIFY begins.
- P2-V0.23.4 delivery modes remain unchanged: source-only validation does not imply Vercel deployment; Vercel Sandbox may remain the current isolated execution provider.
- REVIEW remains the autonomous ceiling.

## 3. Repository intelligence extension

The deterministic compatibility analyzer may add these bounded .NET evidence signals:

- `*.sln` at an admissible application/package root;
- `*.csproj` at an admissible application/package root;
- language signal `csharp` for `.cs` files;
- ecosystem signal `dotnet` tied to the manifest path.

Raw `.sln`/`.csproj` contents are evidence only. No target, command, Exec task, package source, URL, environment value, or arbitrary XML content becomes command authority.

A repository with multiple materially conflicting ecosystem roots must remain ambiguous unless an existing deterministic application-root contract resolves one exact root without model/user choice.

## 4. Server-owned validation profiles

A protected validation profile is immutable released policy containing only server-owned values:

- profile ID/version;
- admitted ecosystem;
- executable command;
- fixed argument tuple per BUILD/TEST/VERIFY;
- bounded working directory derived only from the admitted application root;
- timeout bounds;
- required toolchain identity;
- optional fixed dependency-preparation executable/argument template and exact package-domain allowlist.

### Python

The initial Python profile preserves the established Parallax protected BUILD/TEST/VERIFY command contract rather than silently broadening it to arbitrary Python repositories. A Python-looking repository that does not satisfy the established deterministic Parallax markers fails closed until a separately governed generic Python validation profile is admitted.

### Node

Use only fixed Node/npm commands admitted by server policy. Repository `scripts` fields may be compatibility evidence but are never copied into an execution spec. If a safe fixed validation route cannot be proven without executing repository-defined script text, the profile fails closed rather than invoking it.

### .NET

Use fixed `dotnet` CLI commands against the deterministically admitted solution/project root. The profile may select a unique `.sln` or `.csproj` identity as a bounded path argument, but no repository-supplied command text, MSBuild property, target, environment value, package source, or shell fragment is accepted.

The initial .NET profile uses:

- PREPARE: fixed `dotnet restore <target> --nologo` under the exact server-owned NuGet domain allowlist;
- BUILD: fixed `dotnet build <target> --no-restore --nologo` after network lock-down;
- TEST: fixed `dotnet test <target> --no-restore --nologo` after network lock-down;
- VERIFY: fixed `dotnet test <target> --no-restore --nologo` after network lock-down.

TEST/VERIFY deliberately do not depend on BUILD outputs persisting across lifecycle requests because accepted-lineage BUILD, TEST and VERIFY may each execute in a fresh disposable sandbox. Rebuilding locally from already restored packages is allowed; network restore is not.

## 5. Bounded dependency preparation

Dependency preparation is a subordinate execution prerequisite, not an Engineering Run stage.

For a profile that requires PREPARE:

1. Parallax restores the same server-pinned execution snapshot and transfers the exact candidate or accepted-lineage source into a disposable sandbox.
2. Parallax proves the required executable is available using a fixed server-owned toolchain probe.
3. The sandbox network policy permits only the immutable profile package-domain allowlist. No application environment, user secret, provider credential, Git credential, deployment credential, or model/user value is injected.
4. Parallax runs only the profile's fixed PREPARE executable and argument template against the deterministically admitted target.
5. A nonzero/timeout/unavailable PREPARE result produces bounded readiness evidence and stops without source-lineage acceptance or lifecycle success.
6. On success, Parallax changes the sandbox network policy to `deny-all` and requires the SDK/session state to confirm the new policy before any BUILD/TEST/VERIFY command executes.
7. BUILD/TEST/VERIFY then run with the existing empty environment and deny-all network contract.

Package restore may evaluate repository build metadata as part of the toolchain's normal restore behavior. That untrusted behavior remains confined to the disposable no-secret sandbox and cannot become command, credential, lineage, deployment, or approval authority.

A future optimization may produce a content-addressed dependency snapshot/cache after successful PREPARE, but this slice does not require such persistence and must not weaken the policy transition above.

## 6. Security and authority requirements

- Profile selection consumes only canonical Project/run/lineage-bound server materialization.
- Every selected path must be normalized relative, remain under the protected workspace, reject symlink/path escape, and satisfy existing source bounds.
- Command/executable/argument templates are code constants or equivalent immutable server registry entries.
- Before accepted-lineage reconstruction, the caller-supplied `ExecutionSpec` must still match the existing server-owned stage authorization envelope; arbitrary/spoofed arguments are rejected before source materialization.
- No generic shell (`sh -c`, `bash -c`, PowerShell command text), arbitrary subprocess string, manifest script expansion, or model-proposed invocation is introduced.
- PREPARE network authority is exact-domain, profile-owned, temporary, no-secret, and unavailable to BUILD/TEST/VERIFY.
- No application secrets, provider credentials, deployment credentials, environment propagation, Git mutation, or production authority.
- Candidate validation remains disposable and cannot call lineage acceptance.
- Accepted-lineage execution remains exact-lineage bound and uses the identical profile ID/digest selected for candidate validation.
- A missing required executable, failed dependency preparation, or unavailable dependency is a protected readiness failure with bounded typed evidence, not a reason to fall back to a different ecosystem or ungoverned command.
- Unsupported or ambiguous repositories fail before profile command execution.

## 7. Acceptance criteria

### AC-01 Deterministic .NET compatibility evidence
Repository intelligence recognizes representative `.sln`, `.csproj`, and C# source evidence deterministically, records bounded evidence identities, and does not execute or trust repository command text.

### AC-02 Fixed server-owned toolchain profiles
Python, Node, and .NET profiles contain only server-owned command templates. Repository/model/user text cannot change executable, argument template, environment, network, provider, Git, deployment, or approval authority.

### AC-03 Deterministic fail-closed selection
An exact source tree produces one stable profile/root identity or a typed unsupported/ambiguous result. Mixed/conflicting roots never trigger guessed execution.

### AC-04 .NET protected validation
A representative OT Time-like C# repository selects the .NET profile; dependency preparation uses only fixed `dotnet restore` with the exact NuGet domain allowlist; candidate BUILD/TEST/VERIFY then use fixed offline `dotnet` invocations against the admitted solution/project identity instead of Parallax pytest paths.

### AC-05 Python behavior does not regress
Representative existing Python/Parallax fixtures retain the exact established protected BUILD/TEST/VERIFY behavior and all inherited execution-policy tests remain green. Generic Python repositories are not silently admitted through broadened commands.

### AC-06 Node remains bounded
A representative Node repository is either validated through a fixed admitted Node profile or explicitly rejected when safe fixed commands cannot be proven. Repository `scripts` values never become command authority.

### AC-07 Candidate and canonical validation agree
The selected validation profile identity is bound to candidate-validation evidence and accepted-lineage BUILD/TEST/VERIFY; canonical execution cannot silently switch profile/toolchain after candidate admission.

### AC-08 Dependency/toolchain readiness fails closed
Missing executable, failed PREPARE, inability to lock the network to deny-all, or unavailable dependency returns bounded non-success evidence and performs no source-lineage acceptance, Git/provider mutation, deployment mutation, or fabricated lifecycle progress.

### AC-09 Existing authority and delivery contracts remain intact
Project/spec/run identity, single-writer source acceptance, immutable lineage, evaluator precedence, tenant/auth, source-only handoff, Vercel Preview delivery, provider scopes, and REVIEW boundaries do not regress. Spoofed stage specs remain rejected before accepted-lineage materialization.

### AC-10 OT Time authenticated production proof
After exact release deployment, a QA-owned source-only Project bound to `github:Ryan9876/ot-time` progresses from PLAN through IMPLEMENT/BUILD/TEST/VERIFY to REVIEW and the authenticated exact-lineage ZIP handoff verifies, without Vercel deployment target, Vercel repository-provider authorization, out-of-band source edit, owner impersonation, or DB mutation.

## 8. Scope

Expected implementation areas:

- `services/api/parallax_api/code/repository_intelligence.py` — narrow .NET evidence support;
- server-owned validation profile/selection policy;
- `services/api/parallax_api/code/agentic_runtime.py` — candidate validation profile selection, bounded PREPARE, and profile evidence;
- same-lineage execution — pre-materialization authorization-envelope validation, exact profile selection, bounded PREPARE, network lock-down, and profile evidence;
- focused tests for Python/Node/.NET selection, ambiguity, injection resistance, preparation/network lock-down, and candidate/canonical agreement;
- W8-S2 OT Time production replay.

Exact files may vary after implementation diagnosis. Unrelated refactoring is not authorized.

## 9. Non-goals

- Arbitrary repository script execution.
- Generic user/model-provided shell commands.
- Network access during BUILD/TEST/VERIFY.
- Unrestricted network access during dependency preparation.
- Injecting package-manager credentials or private-feed secrets in this slice.
- Replacing Vercel Sandbox in this slice.
- Implementing IIS/Azure/AWS/local deployment adapters.
- Changing P2-V0.23.4 delivery-mode semantics.
- Supporting every language/toolchain in one release.
- Benchmark-specific or OT-Time-specific runtime branches.

## 10. Release gate

1. Authentic DSPy SpecCritic + SpecCompiler execution and committed `specs/compiled/P2-V0.23.5.plan.json`; strict `validate_spec.py --require-dspy` must pass after this dependency-preparation contract change and before the related semantic runtime implementation.
2. Focused deterministic profile/negative/security/preparation tests pass.
3. Existing repository-intelligence, sandbox, agentic-runtime, source-lineage, provider, auth, delivery, benchmark, and protected-promotion regressions remain green.
4. Exact-head Workstream Spec Validation, Bounded Autonomy Pilot, and full P2 CI pass.
5. Merge only the exact validated head.
6. Production API reaches READY on the exact release source; existing provider/source/lineage/execution preflights remain green or are deliberately extended without authority widening.
7. Authenticated OT Time source-only replay reaches REVIEW and exact-lineage ZIP handoff.
8. `ARCHITECTURE.md` updates if validation profile selection/preparation becomes a durable runtime contract. `CURRENT-STATE.md` updates only after exact deployment/replay evidence. `DESIGN-SYSTEM.md` and `PROJECT-CONSTITUTION.md` change only if their durable subject matter changes.
