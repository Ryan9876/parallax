{
  "$schema": "https://openapi.vercel.com/vercel.json",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./",
  "buildCommand": "python scripts/production_provider_preflight.py && mkdir -p public && printf 'parallax-api-build\\n' > public/build-marker.txt",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
