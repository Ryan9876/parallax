{
  "$schema": "https://openapi.vercel.com/vercel.json",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./",
  "buildCommand": "python scripts/production_provider_preflight.py && python scripts/production_projected_source_preflight.py && if [ \"${VERCEL_ENV:-}\" = \"production\" ]; then python -m pip install --disable-pip-version-check --quiet 'vercel>=0.9,<0.10' && python scripts/production_blob_sdk_preflight.py; else echo 'Production Blob SDK preflight: SKIP (non-production)'; fi && mkdir -p public && printf 'parallax-api-build\\n' > public/build-marker.txt",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
