# [WS] Mobile Interaction Model & Guided Build Flow

ID: `WS-MOBILE-GUIDED-BUILD`
State: **ACTIVE**
Spec: `specs/P2-V0.18.7.md`
Branch: `ws/mobile-guided-build-flow`
Base: `7b65aff9ed62ddc94b1a29eb22c5ab81048888bf`

## Objective

Replace the confusing compact desktop composition with a mobile-specific Chat / Build / Project flow before Wave 6, without changing server-owned engineering authority or desktop behavior.

## Owned paths

- `apps/client/src/App.tsx`
- bounded compact behavior in `apps/client/src/components/WorkSpecificationStatus.tsx`
- bounded compact behavior in `apps/client/src/components/EngineeringRunStatus.tsx`
- bounded compact behavior in `apps/client/src/components/EditorialWorkspaceHeader.tsx`
- bounded compact behavior in `apps/client/src/components/EditorialProjectWorkspace.tsx`
- new `apps/client/src/components/mobile/**`
- bounded mobile composition under `apps/client/src/components/observability/**`
- bounded mobile acceptance under `apps/client/scripts/**`
- `specs/P2-V0.18.7.md` and compiled plan

## Shared paths

`DESIGN-SYSTEM.md` and `CURRENT-STATE.md` only if their durable subject changes. Avoid root/shared contracts.

## Dependencies

Wave 5 generalized delivery and stabilization through #127; existing Project, Work Specification, Engineering Run and observability APIs.

## Acceptance gate

Spec validation, applicable DSPy policy gate, client typecheck/state/export, browser/Skia acceptance, phone-width deterministic acceptance, and exact-head repository release gates required for a release candidate.

## Collision policy

Do not broaden into API/runtime changes. If a server behavior change is required, stop and record a dependency rather than silently expanding scope.

## Completion

Validated PR ready for Integration / Control Tower. No merge/deploy by worker default.
