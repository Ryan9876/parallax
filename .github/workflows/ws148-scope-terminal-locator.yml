name: WS148 Scope Terminal Locator

on:
  push:
    branches: [ws/w4-live-build-ux]
    paths: ['.github/workflows/ws148-scope-terminal-locator.yml']

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ws/w4-live-build-ux
      - run: python scripts/ws148_scope_terminal_locator.py
      - run: |
          git config user.name 'Parallax Release Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add apps/client/scripts/live-build-smoke.mjs
          git commit -m 'Scope Live Build Terminal smoke locator'
          git push origin HEAD:ws/w4-live-build-ux
