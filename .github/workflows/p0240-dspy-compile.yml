name: P2-V0.24.0 DSPy Compile

on:
  push:
    branches: [ws/p0240-platform-overhaul]
    paths:
      - 'specs/P2-V0.24.0.md'
      - 'scripts/compile_spec.py'
      - 'services/api/parallax_api/intelligence/dspy_programs.py'
      - '.github/workflows/p0240-dspy-compile.yml'

permissions:
  contents: write

concurrency:
  group: p0240-dspy-compile
  cancel-in-progress: true

jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ws/p0240-platform-overhaul
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip
          cache-dependency-path: services/api/pyproject.toml
      - run: pip install -e 'services/api[dev]'

      - id: model
        name: Select provider-backed or local DSPy model
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          if [ -n "$OPENAI_API_KEY" ]; then
            echo "provider=true" >> "$GITHUB_OUTPUT"
            echo "DSPY_MODEL=openai/gpt-5.6-sol" >> "$GITHUB_ENV"
          else
            echo "provider=false" >> "$GITHUB_OUTPUT"
            echo "DSPY_MODEL=ollama_chat/qwen2.5:0.5b" >> "$GITHUB_ENV"
            echo "DSPY_API_BASE=http://127.0.0.1:11434" >> "$GITHUB_ENV"
            echo "DSPY_API_KEY=" >> "$GITHUB_ENV"
            echo "DSPY_LOCAL_DEVELOPMENT=1" >> "$GITHUB_ENV"
          fi

      - name: Install local DSPy development LM
        if: steps.model.outputs.provider == 'false'
        run: |
          curl -fsSL https://ollama.com/install.sh | sh
          nohup ollama serve > /tmp/ollama.log 2>&1 &
          for i in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:11434/api/tags >/dev/null; then break; fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:11434/api/tags >/dev/null
          ollama pull qwen2.5:0.5b

      - name: Compile exact P2-V0.24.0 specification
        timeout-minutes: 8
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python scripts/compile_spec.py specs/P2-V0.24.0.md

      - name: Verify authentic compiled evidence
        run: python scripts/validate_spec.py --require-dspy specs/P2-V0.24.0.md

      - name: Commit generated plan only
        run: |
          set -euo pipefail
          git config user.name "parallax-workstream-compiler"
          git config user.email "actions@users.noreply.github.com"
          git add specs/compiled/P2-V0.24.0.plan.json
          if git diff --cached --quiet; then
            echo "Compiled plan already current"
            exit 0
          fi
          git commit -m "Compile P2-V0.24.0 DSPy plan"
          git push origin HEAD:ws/p0240-platform-overhaul
